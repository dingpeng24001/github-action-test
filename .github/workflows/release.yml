name: release

env:
  VERSION: ${GITHUB_REF#refs/*/}

on: 
  release:
    types: [created]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: version 
        run: echo ${VERSION}
